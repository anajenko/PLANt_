<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PLANt"
        tools:targetApi="31">

        <activity
            android:name=".ActivitySPOK"
            android:exported="true"
            android:launchMode="singleTask"
            android:label="Splošni podatki o kmetiji">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityUrnik"
            android:label="Urnik"/>
        <activity android:name=".WeekViewActivity" />
        <activity android:name=".EventEditActivity" />
        <activity android:name=".ActivityRazpisi"
            android:windowSoftInputMode="adjustPan"
            android:label="Razpisi"/>
        <activity
            android:name=".Razpis1"/>
        <activity
            android:name=".Razpis2"/>
        <activity
            android:name=".Razpis3" />
    </application>

</manifest>